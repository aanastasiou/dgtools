Swap two numbers

Load initial data
>++++++++>++++++++++++ Tape now looks like tape={0 8 12 0} (at n=11)

"Save" tape(2) to 
an intermediate 
cell
[->+<]                 Tape now looks like tape={0 8 0 12} (at n=164)

Now do 
tape(2)=tape(1)
<[->+<]                Tape now looks like tape={0 0 8 12} (at n=272)

Now do
tape(1)=tape(3)
>>[-<<+>>]             Tape now looks like tape={0 12 8 0} (at n=504)
