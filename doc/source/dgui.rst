Orchestrating compilation via ``dgui.py``
=========================================

The `dgtools` toolchain is composed of three simple programs runnning from the command line.

These can be used individually or through the GUI.
